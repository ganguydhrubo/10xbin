'use client';
const groups=[{label:'01 / CORE ENGINES',items:['Python 3.12','FastAPI','Rust Extensions','n8n Core','PostgreSQL','Supabase','Neon Serverless']},{label:'02 / INFERENCE & TELEPHONY',items:['Groq LPUs','Google Gemini 1.5 / 2.0 Pro','Claude 3.5 Sonnet','OpenAI GPT-4o','Vapi','Twilio SIP Trunking']},{label:'03 / REAL-TIME & EDGE',items:['WebSockets','Redis','Next.js Edge Runtime','Docker','Kubernetes']}];
export default function TechStack(){return <section className="section stack-section" id="stack"><div className="section-heading"><div><div className="eyebrow">// HARDWARE & RUNTIME STACK</div><h2>Technologies<br/><span>Under Management.</span></h2></div><span className="stack-label">PURPOSE-BUILT. INTEROPERABLE.</span></div><div className="stack-groups">{groups.map(g=><div className="stack-group" key={g.label}><h3>{g.label}</h3><div className="capsules">{g.items.map((item,i)=><span className="capsule" key={item} onPointerMove={e=>{const r=e.currentTarget.getBoundingClientRect();e.currentTarget.style.setProperty('--x',e.clientX-r.left+'px');e.currentTarget.style.setProperty('--y',e.clientY-r.top+'px')}}><i>{String(i+1).padStart(2,'0')}</i>{item}<b>↗</b></span>)}</div></div>)}</div></section>}
