'use client';
import {useEffect,useRef,type MouseEvent} from 'react';
import {Cpu,AudioLines,Network,ShieldCheck,ArrowUpRight} from 'lucide-react';
function NodeCanvas(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const c=ref.current!,ctx=c.getContext('2d');if(!ctx)return;let frame=0;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const draw=(t:number)=>{c.width=c.clientWidth*devicePixelRatio;c.height=c.clientHeight*devicePixelRatio;ctx.scale(devicePixelRatio,devicePixelRatio);const w=c.clientWidth,h=c.clientHeight;ctx.clearRect(0,0,w,h);const nodes=Array.from({length:34},(_,i)=>({x:w*.5+Math.cos(i*2.399+(reduced?0:t*.00007))*Math.sqrt(i/34)*w*.44,y:h*.5+Math.sin(i*2.399+(reduced?0:t*.00007))*Math.sqrt(i/34)*h*.43}));nodes.forEach((n,i)=>{nodes.slice(i+1).forEach(m=>{const d=Math.hypot(n.x-m.x,n.y-m.y);if(d<w*.23){ctx.strokeStyle=`rgba(0,240,255,${.21*(1-d/(w*.23))})`;ctx.beginPath();ctx.moveTo(n.x,n.y);ctx.lineTo(m.x,m.y);ctx.stroke()}});ctx.fillStyle=i%5===0?'#b7ffff':'#168692';ctx.beginPath();ctx.arc(n.x,n.y,i%5===0?2.5:1.5,0,Math.PI*2);ctx.fill()});frame=requestAnimationFrame(draw)};frame=requestAnimationFrame(draw);return()=>cancelAnimationFrame(frame)},[]);return <canvas ref={ref} className="node-canvas" aria-label="Simulated neural compute node clustering"/>}
const modules=[{name:'Compute Engine',node:'ALPHA',icon:Cpu,description:'High-throughput inference. Deterministic execution. An intelligence layer built for scale.',metric:'128',unit:'PARALLEL NODES',kind:'compute'},{name:'Voice & Telephony',node:'BETA',icon:AudioLines,description:'Autonomous voice arrays that listen, reason, and respond across every conversation.',metric:'24 / 7',unit:'CONTINUOUS INTAKE',kind:'voice'},{name:'Distributed Data Mesh',node:'GAMMA',icon:Network,description:'Multi-tenant event streams, real-time WebSockets, and distributed ACID persistence.',metric:'∞',unit:'CONNECTED WORKFLOWS',kind:'data'},{name:'Sovereign Security',node:'DELTA',icon:ShieldCheck,description:'Sovereign encryption, air-gapped deployment architecture, and zero-trust monitoring.',metric:'ZERO',unit:'IMPLICIT TRUST',kind:'security'}];
export default function BentoProfile(){const move=(e:MouseEvent<HTMLElement>)=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=e.currentTarget.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;e.currentTarget.style.setProperty('--x',x+'px');e.currentTarget.style.setProperty('--y',y+'px');e.currentTarget.style.transform=`perspective(1200px) rotateX(${(y/r.height-.5)*-5}deg) rotateY(${(x/r.width-.5)*5}deg)`};return <section className="section" id="architecture"><div className="section-heading"><div><div className="eyebrow">// SYSTEM ARCHITECTURE</div><h2>Autonomous<br/><span>Enterprise Fabric.</span></h2></div><p className="intro">One connected intelligence.<br/>From the first signal to the final execution.</p></div><div className="bento">{modules.map(({icon:Icon,...m},i)=><article className={'bento-card '+m.kind} key={m.node} onMouseMove={move} onMouseLeave={e=>{e.currentTarget.style.transform='';}}><div className="card-top"><span className="micro">NODE {m.node} / 0{i+1}</span><Icon size={19}/></div><div className="card-visual">{i===0?<NodeCanvas/>:i===1?<div className="waveform">{Array.from({length:57},(_,n)=><i key={n} style={{height:`${12+Math.abs(Math.sin(n*.67))*Math.sin(n/57*Math.PI)*78}%`,animationDelay:`${n*-.09}s`}}/>)}</div>:i===2?<div className="mesh-diagram"><span>INGEST</span><i/><strong>DATA MESH</strong><i/><span>EXECUTE</span><small>EVENT BUS · REAL-TIME · PERSISTENCE</small></div>:<div className="security-visual"><ShieldCheck size={64} strokeWidth={.8}/><span>TRUST BOUNDARY ACTIVE</span></div>}</div><div className="card-title"><h3>{m.name}</h3><ArrowUpRight size={18}/></div><p>{m.description}</p><div className="card-metric"><b>{m.metric}</b><span>{m.unit}</span><i className="status-dot"/></div></article>)}</div><div className="section-note">// ARCHITECTURE VISUALIZATIONS & TELEMETRY ARE SIMULATED</div></section>}
